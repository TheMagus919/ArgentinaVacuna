extends ../layout.pug

block content
    .contenedorVolverCrear
        a.link(href="/traslado/crear")
            | Volver Atrás
            img(src='/images/volver.png', alt="Flecha hacia atrás")
    h2 Listado de Lotes.
    p Selecciona un el Lote que sera trasladado a otro Centro de Vacunacion, si no aparece ninguno se debera a que no cuenta con vacunas disponibles.
    table.table#tabla
        thead
            tr
                th NUMERO DE LOTE
                th NOMBRE COMERCIAL
                th CANTIDAD DE VACUNAS
                th OPCIONES
        tbody
            each lote in listaDeLotes
                tr
                    td= lote.nroLote
                    td= lote.nombreComercial
                    td= lote.dataValues.cantidad_total
                    td
                        form(action=`/traslado/crear/solicitud/${lote.nroLote}`, method="get")
                            input.btn.btn-primary(type="submit", value="Seleccionar") 
